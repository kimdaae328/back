<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.youeatieat.mapper.ProductMapper">
    <select id="selectList">
       select id, product_name, product_price, product_quantity, product_category, product_title_image_url, product_info_image_url, product_min_number, product_status, member_id, created_date, updated_date
       from tbl_product
        where product_status = 'active'
    </select>

    <select id="selectById">
        select id, product_name, product_price, product_quantity, product_category, product_title_image_url,
               product_info_image_url, product_min_number, product_status, member_id, created_date, updated_date
        from tbl_product
        where id = #{id}
    </select>
</mapper>