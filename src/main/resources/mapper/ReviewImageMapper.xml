<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.youeatieat.mapper.ReviewImageMapper">
    <select id="selectReviewImageByReviewId">
        select id, review_image_url, review_image_sort_order, review_id, review_image_status
        from tbl_review_image
        where review_id = #{reviewId}
        order by review_image_sort_order
    </select>

</mapper>